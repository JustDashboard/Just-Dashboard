# Frontend feature map

The App Router keeps route entry files thin where a feature has reusable panels and colocates page-only
orchestration where splitting it would hide the workflow. Backend capability checks remain authoritative;
hiding a control through `useAuth().can()` is affordance only.

| Route area | UI responsibility | Primary implementation |
| --- | --- | --- |
| `/login` | One centred column: password, TOTP challenge, enrolment where the install requires it, recovery codes, and partial-session states | `src/app/login/page.tsx`, auth hooks, shared logo/state/controls |
| `/` | Host overview: five capacity tiles (CPU, memory, load, network, and the fullest filesystem), a health list that folds failed systemd units and unhealthy or restarting containers into the recorder's findings, an hour of sparklines, recent events, and one destination tile per module — Docker, databases, proxy, security exposure, package updates and reboot state, deployments, backups, and the dashboard's own version | dashboard root page plus `components/metrics/` |
| `/dashboard` | The panel's own version, self-update status and searchable release notes. The sidebar and the page call it "Settings" — inside a product called Dashboard, an entry called Dashboard said nothing | `components/update/`, self-update provider |
| `/dashboard/configuration` | The panel's own settings — address, certificate mode, ports, allowlist, two-factor policy, session lifetimes — plus restart, rebuild, and a run record that is followed across the restart it describes. Picking a certificate mode carries the address, listening interface and allowlist with it, filled from the machine's own tailnet identity | `components/config/restart-progress.tsx`, `hooks/use-self-config.tsx` (`system.admin` only) |
| `/account`, `/account/security`, `/account/sessions`, `/account/keys`, `/account/users` | The signed-in account as five pages under one strip. Profile: the display name as the title, a readings row of two-factor, sessions, API keys and (for `system.admin`) users — each a `StatLink` to its page — the identity block (picture cropped and shrunk in the browser before upload, display name, sign-in name) and what the role can do, one capability per row. Security: password and two-factor. Sessions: each device described from its user agent, sign out one or every other. API keys: what a key is for and the request that uses it, then the keys with their state, and a create dialog that explains each role and hands over the key once with a copyable `curl`. Users: every account with picture and both names, role and sign-in inline, rename/password in a dialog, 2FA reset and typed deletion | `components/account/` (`user-avatar.tsx` is also the rail's picture), account layout/pages, auth hook, confirmation primitives |
| `/audit` | Filtered/paginated mutation audit trail | audit page and shared page/panel/state controls |
| `/backups` | Drawn like the host Overview: four readings (jobs, last backup, next backup, stored), an attention list (a job whose last run failed, a job that is overdue, what the server has that no job covers), the jobs as a plain table that becomes rows below `2xl`, and a Coverage list of everything the dashboard knows about — Docker volumes, compose stacks, deployments, repositories, saved databases, the proxy's configuration and the dashboard itself — each protected by a named job or carrying a **Back up** button that opens the form pre-filled with its paths, SQLite file, native dump and the containers to pause. A job opens in a sheet (`?job=`) with its facts, its verbs (run, edit, pause/resume, test destination, delete) and its runs; a run offers its log, an archive browser with per-entry selection, restore into a directory or back in place, database restore into the connection or a drill database, the recovery check and a download. The form picks sources from the same coverage list, builds the schedule from presets with the sentence and next firings under it, keeps by count and by age, and lists the containers to freeze during the archive. `?source=<path>` opens the form with that path | backups page plus `components/backups/`; backend contract in [`../backend/git-backups-users.md`](../backend/git-backups-users.md#backups) |
| `/databases/*` | The connection as the section's title (a switcher) and a facts row, with the section's pages on the rail (registered through `nav-scope.tsx`, so a non-SQL engine's panel drops the pages it has no use for and every entry carries `?conn=`); Browse and Structure as one full-height workbench each — a shared table rail (`table-rail.tsx`) beside the grid or the column/index/key/definition view — with every table verb declared once in `table-actions.tsx`; the ER diagram as a workbench with an inspector, a per-table menu (browse, structure, query, focus, isolate, hide, colour, note), search-and-pan, three levels of detail, layout direction and spacing, tidy, snap, lock, full screen, and export as PNG, SVG, Mermaid, DBML or JSON, with its arrangement saved per connection and schema (`diagram/memory.ts`); query with a URL-supplied statement, cross-table find, monitor, ORM generation, and a connection page of readings, the connection string (masked, shown or copied on request, plus the public-address form for a server on this host), facts and maintenance rows — a dump, an Open to the internet switch for a container this host publishes, Remove only for a connection the sync would not re-add, and Delete with the option to remove the container and its data. New database starts an engine in a container with a Reachable from anywhere switch that is on by default. Every dialog is built from `components/form.tsx` and shows the statement it will run | `components/database/` and database layout/pages |
| `/deploy`, `/deploy/notifications`, `/deploy/credentials`, `/deploy/new`, `/deploy/[id]` (+ `/deployments`, `/logs`, `/runtime`, `/console`, `/players`, `/game-settings`, `/settings/{general,build,runtime,variables,domains,storage,databases,automation,danger}`), `/deploy/[id]/runs/[run]` | Projects grid with in-progress runs, search and state chips, and the archived list with Restore and Delete permanently; fleet-level notification channels and saved credentials (Git tokens, SSH keys, registry logins, provider tokens — sealed, testable against a repository or image, refused for removal while in use) as rows under three readings (held, in use, GitHub App) with the GitHub App panel above them (create through GitHub's manifest flow, installations, install-elsewhere link, webhook address, disconnect); one-page project creation from a Git repository, image, template, database, Compose stack or existing workload, with unfinished setups offered for resumption, the detected framework named, the variables the source reads prefilled with their example placeholders and sources (a Generate button for secret-shaped names, `APP_KEY` minted for Laravel), each detected database offered as one button, the single-page switch for static output, a Python interpreter field, the GitHub App's repositories first in the import picker (tagged App, cloned through the installation's credential), a password switch on the public address, and `?repo=` prefilling the Git tab from a deploy link; the project shell (status word, Visit, one command, a verbs menu with Stop/Start, Redeploy, Rebuild, Deploy a specific version, Duplicate project, Open in Docker, Archive) with Overview, Deployments (delivery insights, run rows with rollback, comparison, pin and older pages), Logs (five traffic readings and an alerts line over one pane of three views — Requests from the ingress's access log with status-family chips and counts, a Pages-only chip, CSV export, the house chart of request volume with 4xx/5xx lines and the p95 under it, deploy/restart/exit marks and drag-to-narrow, rows that open in place onto the container's output and events around that minute, and a Live toggle that prepends; Insights, the window faceted as bar lists — pages with their own p95 and 5xx, clients with scanners named and a Block verb, agents with the bot share, referers, status codes, slowest, bytes served; Events, Docker's exits, OOM kills, restarts and health flips over containers and networks, searchable, narrowable by kind, followed live, scopable to a moment, each row linking to the run that made the release and — where the audit log explains it — to the entry that caused it, with the view and the moment in the URL), Runtime, Console, game Players and Server settings, and nine settings sections of setting cards (General carries the Source card that changes a repository, branch, root or image in place; Databases opens on four readings — linked count, connection, backup policy and native-dump coverage — over linked databases as rows carrying the engine, the database, the managed hostname, the observed status, a Test connection verb and, for an administrator, Copy application URL and Remove, with the variables that carry each database named and a removal that offers to delete them because runtime activation attaches a database by reading the variable rather than the dependency row; each declared backup job shows its schedule, last success, next run and stored size with the live release's observation folded onto the same row, Run now, and a warning naming any linked database the job takes no native dump of with one press to add it, or a **Back up** link to `/backups?database=<id>` when no job is declared at all; volumes carry their observed mount state); the deployment page with the release timeline, the build console, runtime logs, details rows that open to each step's evidence, and metrics around activation with sparklines; project cards and the project header carry the website's own icon; legacy `?tab=` links redirect | `components/deploy/` (`projects-page.tsx`, `archived-projects.tsx`, `notifications-page.tsx`, `credentials-page.tsx`, `new-project.tsx` + `new-project/`, `project-context.tsx`, `project-shell.tsx`, `project-overview.tsx`, `project-deployments.tsx`, `project-logs.tsx`, `requests-workspace.tsx`, `request-console.tsx`, `request-chart.tsx`, `traffic-facets.tsx`, `lifecycle-feed.tsx`, `run-traffic.tsx`, `settings/traffic-alerts.tsx`, `project-runtime.tsx`, `project-console.tsx`, `game/`, `run-page.tsx`, `run-pipeline.tsx`, `run-steps.tsx`, `run-metrics.tsx`, `run-logs.tsx`, `site-preview.tsx`, `project-mark.tsx`, `project-wiring.tsx`, `wire.tsx`, `settings/`, `vocabulary.tsx`); contract in [`../deployments/implementation.md`](../deployments/implementation.md) and [`../deployments/request-observability.md`](../deployments/request-observability.md) |
| `/docker/*` | Overview plus containers, images, networks, volumes, compose stacks, event history, creation, diagnostics, and resource detail. Every page is drawn the way the host Overview is — readings and hairline lists on the page, no framed block anywhere, each block rising once when its data lands (`design-system.md` §15). The overview names what is not running rather than only counting it, and draws disk as a breakdown; the containers and stacks pages both filter by state; the containers, images, volumes and networks tables are each replaced — not narrowed — by a list of the same rows below the width where the table stops fitting (`xl` for containers and images, `lg` for volumes and networks); and each container's and stack's verbs are offered as words — declared once in `container-actions.tsx` and drawn by the table row, the card, the stack's overflow menu and the detail panel alike | `components/docker/`; see [`features-terminal.md`](features-terminal.md#feature-panels) |
| `/files` | One framed workbench (design-system §15): a fixed sidebar of places — home, the roots, `/etc`, `/var/www`, the accounts — then the starred folders and the recent ones, which stays put while the listing walks into folders and marks the one being browsed (`files-sidebar.tsx`; the same list is one menu on a phone, `places-menu.tsx`); a listing as details or tiles with image thumbnails and video posters on every row (`thumbnail.tsx`), Ctrl/Shift selection, arrow-key navigation, a right-click menu whose verbs are declared once (`file-actions.tsx`) and a folder menu between rows; drag-and-drop between rows, crumbs and sidebar places (`dnd.ts`); uploads of files and whole folders with per-file progress and a replace/keep-both/skip question for taken names (`uploads.tsx`, `conflict-dialog.tsx`); a resizable inspector; a full-screen viewer for pictures, video, audio, PDF, text and archives (`media-viewer.tsx`); Monaco editing with full screen and a diff review before saving; image editing, archives, permissions, a footer with the disk's free space, and Git/shell handoffs | files page and `components/files/`; checked by `tests/browser/files-ui.spec.ts` |
| `/git` | A list drawn the way the host Overview is — four readings (repositories, uncommitted, behind, unpushed), filter chips, a plain table that becomes rows below `lg`, and the chosen repository in `?repo=` — with clone and init behind **Add repository**; then one framed workbench per repository: the file tree, a Changes / History / Branches / GitHub column and a preview column separated by hairlines, resizable, with the repository's reading and its verbs in one strip. Changes lists a file on both sides when it is staged and edited again, keeps stashes at the top, deletes untracked files on discard, offers amend and the committer identity; History searches, pages and narrows to one file, and each commit opens as its message and file list with per-file diffs, branch/tag/cherry-pick/revert/undo verbs; Branches carries merged/gone facts, switch, check out a remote branch, merge, compare, rename, delete here or on the remote, and the tags; GitHub carries the pull requests with review and check state, merge and check-out, the Actions runs on the branch and the new-request form with what it would carry | `components/git/`; backend contract in [`../backend/git-backups-users.md`](../backend/git-backups-users.md#git-working-copies) |
| `/logs` | One-frame workbench: hideable, resizable source rail beside a single pane with Live/History tabs, one filter with level chips and counts, histogram, columned line console, retention footer, and export | logs page and `components/logs/` |
| `/metrics` | Redesigned per design-system §15: a facts row (processor, memory, swap, uptime, recording cadence), ten headline tiles read from the newest frame — CPU, memory, load, pressure, network, disk I/O, storage, sockets, processes, and the hottest sensor or the open file handles — each carrying a delta against the previous window on a recorded range; the recorder's findings as a plain list; a notable-moments list (peaks, stalls, failed deploys, reboots) whose rows zoom the charts; top processes by CPU, memory or I/O; plain utilisation and saturation charts (live and recorded, storage/inodes, annotations, range stack, synchronized cursor); and per-core, temperature, filesystem and interface readings. A dragged span is mirrored into the URL as `from`/`to` so a window is a link, the live feed can be paused, and the window on screen exports as CSV | metrics page, `components/metrics/` (`notable-moments.tsx`, `top-processes.tsx`, `hardware-panels.tsx`), `lib/metrics-summary.ts`, `lib/metrics-export.ts` |
| `/packages` | Drawn per design-system §15: the manager, the index age and the update verdict as a facts row under the title; four readings (installed, installed by hand, updates, on disk — naming the largest package); security updates waiting, a reboot owed and a stale index as notices that carry their own button; and three views under one underlined strip, each a plain panel — the installed table with by-hand/everything/behind chips and a size sort, the updates table with its upgrade-all verb, and the live catalogue search with a one-press install per row — with a row's tags at its edge, a sheet of facts and usage behind every name, and resumable jobs | packages page, `components/packages/`, shared job console; checked by `tests/browser/packages-ui.spec.ts` |
| `/processes/*` | Four routed pages listed on the rail's Processes panel. Live: four host readings (processes, blocked, zombies, unmanaged) over the inventory table with owner chips, user/state facets and an automatic focus that says why; a row's verbs (terminate, kill, pause/resume, hang up, open its supervisor, copy) in one menu; a detail sheet addressed by `?pid=` with listening ports, connection count, open-file limit, meters, a priority stepper and a Tree tab of the parent chain and children. PM2: whether each account's daemon resurrects on boot (hook and last save), four readings, a table with start/reload/restart/stop inline and logs, scale, reset counters, flush, delete behind the menu, a detail sheet of how it runs, a start-application dialog that shows the `pm2 start` command it will run, and daemon-wide save/reload/restart/stop. Services: four readings, state chips and a startup filter, failed units first, clear-failed and enable/disable, a sheet with reload where the unit supports it and a link to its unit file, plus `daemon-reload`. Scheduled: one account's cron jobs as rows with the schedule in words and its next run, per-job enable/disable/edit/remove and an add-job dialog with presets, systemd timers with run-now, and the read-only system cron files | processes layout and pages, `components/procs/`, `components/verbs.tsx`, `lib/cron.ts`, `lib/crontab.ts` |
| `/proxy/*` | Overview with the engine as a facts row (version, unit state, config directory, certbot, renewal), test/reload/restart/start/stop through the Services API, four destination tiles, an attention list folded from expired/expiring/unreadable certificates, a stopped renewal timer, disabled or plain-HTTP sites, unread streams and database ports on a public address (`components/proxy/attention.ts`), and the first sites; Sites as four readings over one filterable table whose verbs are declared once in `site-verbs.tsx` (edit, raw config, open, TLS report, access log, duplicate, enable/disable, delete) with a Docker Caddy route listed without an editor it cannot use, a site form built from `components/form.tsx` that links to issuance when it names a certificate the dashboard has not seen, and password files; Certificates as four readings (count, expiring, expired, renewal) with certbot lineages and their verbs, a staging-passed handoff to the real issuance, `?issue=` prefill, the timer switched on from the page, installed certificates with which site uses each, watched domains with a port, and DNS providers with credential state and removal; a TLS report that runs from `?domain=` with the grade as a tile and findings as a list; streams and ports as readings, filters and verbs, with a port's process one click away | proxy layout/pages and `components/proxy/` |
| `/security/*` | Shared posture, firewall and exposure context (the exposure carries the address this browser arrived from); an overview of how the panel is reached, five area tiles with the figure each area measured, and the posture's findings with their remedies; every area page as readings, findings and plain tables — firewall defaults and rules, staged sshd settings, fail2ban jails with a manual ban and a one-press allowlist for your own address, live connections, the login record folded into attackers, network inventory, and twenty filterable probes that other pages can arrive at with an address filled in. Any address in any list is a block at the firewall or one of three lookups | security layout/pages and `components/security/` |
| `/system-users` | Host account inventory, create/update/delete, groups, lock state, and SSH keys | system-users page; backend contract in [`../backend/git-backups-users.md`](../backend/git-backups-users.md#host-users-and-ssh-keys) |
| `/terminal` | Direct PTY sessions, folders, windows with retained screens and connections while switching, tabs and rows named after what the shell is doing (without the agent's own title glyph) with a working mark, the last-open session and window remembered across navigation, side tools, replay, clipboard upload, renderer, and keyboard customization | terminal page, `components/terminal/`, `components/xterm-pane.tsx`; see [`features-terminal.md`](features-terminal.md#the-terminal-panel) |

The project shell's facts row and Settings → General show read-only production Git monitoring status,
including repository access failures. Branch deployments are automatic after the first deployment;
additional webhooks remain separate integrations. Run labels use a per-project sequence starting at 1,
while permanent run URLs retain their global identifiers. `/deploy/notifications`
(`components/deploy/notifications-page.tsx`) manages Discord, Slack, Telegram, e-mail and signed
webhook channels with event selection, pause/resume, test delivery and delivery history; the Git card
on General carries the GitHub commit-status toggle. Settings → Runtime exposes memory, CPU and process
limits, the restart policy and the health-check editor. The Console tab
(`components/deploy/project-console.tsx`) embeds the Docker exec pane on the live release container
for every non-game deployment. The shared vocabulary (`components/deploy/vocabulary.tsx`) is the one
place a run state, a project state, a source line or a release path is put into words.

Cross-feature navigation is intentional: Files and Docker can open Git or a terminal in context; Git can
open GitHub authentication; deployments embed scoped runtime logs, service metrics, and automatic live
website previews while retaining links to owning Docker, proxy, backup, files, and terminal surfaces.
Query parameters that trigger a one-time terminal/file context are consumed
so refresh does not repeat an action.
