# Frontend feature map

The App Router keeps route entry files thin where a feature has reusable panels and colocates page-only
orchestration where splitting it would hide the workflow. Backend capability checks remain authoritative;
hiding a control through `useAuth().can()` is affordance only.

| Route area | UI responsibility | Primary implementation |
| --- | --- | --- |
| `/login` | One centred column: password, TOTP challenge, enrolment where the install requires it, recovery codes, and partial-session states | `src/app/login/page.tsx`, auth hooks, shared logo/state/controls |
| `/` | Host overview: the machine's identity line (its distribution, processor and hypervisor drawn as their marks, kernel, uptime, processes, the verdict), five capacity tiles (CPU, memory, load and network each carrying its last hour as a sparkline, and the fullest filesystem with its meter), a health list that folds failed systemd units and unhealthy or restarting containers into the recorder's findings beside recent events, and one destination tile per module — Docker (with the products its running containers are), databases (with their engines), proxy, security exposure, package updates and reboot state, deployments, backups, and the dashboard's own version | dashboard root page plus `components/metrics/` (`host-identity.tsx`), `components/product-logo.tsx` |
| `/dashboard` | The panel's own version: one identity line (the mark and the installed version, the GitHub repository and branch it follows, the check's verdict), the upgrade while one runs, and the release history as a searchable timeline. The sidebar entry is "Settings" — inside a product called Dashboard, an entry called Dashboard said nothing — and this page is its "Version" | `components/update/` (`release-timeline.tsx`, `update-progress.tsx`), `components/run-transcript.tsx`, self-update provider |
| `/dashboard/configuration` | The panel's own settings. The stack first — the checkout and its three services drawn as Caddy, Next.js and Go with the addresses they listen on — beside Restart and Rebuild as two cards, and the run record under them with its stages and whole transcript, followed across the restart it describes. Then a form in five railed sections (Address, Certificate, Access, Sessions, Features), each head carrying what the section currently is. The certificate modes are three cards with their products' logos; picking one carries the address, listening interface and allowlist with it, filled from the machine's own tailnet identity | `components/config/restart-progress.tsx`, `components/run-transcript.tsx`, `components/run-phases.tsx`, `hooks/use-self-config.tsx` (`system.admin` only) |
| `/account`, `/account/security`, `/account/sessions`, `/account/keys`, `/account/users` | The signed-in account as five pages under one strip. Profile: the display name in the account's identity line (`HostIdentity` with the picture as its mark — the sign-in name and role, member since, last sign-in, the browser and network this session came in through drawn as themselves, and the two-factor verdict), a readings row of two-factor, sessions, API keys and (for `system.admin`) users — each a `StatLink` to its page, naming what it counts with the things themselves: the sessions' browsers, the keys' products, the administrators' faces — the identity block (picture cropped and shrunk in the browser before upload, display name, sign-in name) and what the role can do, one capability per row beside the sidebar glyph it opens. Security: a form whose section heads sit in a rail — two-factor (its state in the rail; enrol with the grouped secret, copy and open-in-authenticator, recovery codes shown once with copy-all, regenerate, turn off), password (the server's rule lit as it is met, `lib/password.ts`) and where the account is signed in, drawn as its clients with a link to Sessions. Sessions: this device as an identity line, every other session as a row drawn as its browser with its system in the tile's corner, the network its address is on (Tailscale's ranges by its mark, a private range, loopback or the internet — `lib/clients.ts`), whether it passed a code and when it was last seen; sign out one or every other. API keys: four readings (in use, used this week, never used, expiring soon), then the usable keys and the revoked or expired ones as rows drawn as the product their name names (`keyProduct`), with the owner's face on a key an administrator reads that is not theirs, the request that uses a key, and a create dialog whose roles are cards and whose name field shows the mark the key will carry; with no keys the empty state says what one is for. Users: four readings (accounts, administrators, two-factor coverage — amber while an administrator has none — and who signed in this week) over the accounts as cards drawn by their faces that open the editor, with role and sign-in inline and 2FA reset and typed deletion in the card's menu; a new account's role is picked from cards | `components/account/` (`user-avatar.tsx` is also the rail's picture and holds `InitialsMark`, the face of anyone drawn from a bare name; `totp-secret.tsx` is also the sign-in page's enrolment; `role-choice.tsx`), `components/client-mark.tsx` (a session's client drawn as itself, shared with a deployment's requests), `lib/clients.ts`, `lib/password.ts`, account layout/pages, auth hook, confirmation primitives; checked by `tests/browser/account-ui.spec.ts` |
| `/audit` | Filtered/paginated mutation audit trail | audit page and shared page/panel/state controls |
| `/backups` | An attention list (a job whose last run failed, a job that is overdue), the jobs as cards ordered worst first — each drawn as the products it covers, its last run in the colour of its outcome, its destination (Backblaze as itself), its last fourteen runs as a strip, its next run, schedule, storage and retention — with the job count and total stored in the Jobs section header, and a Coverage list under a meter of how much is covered, of everything the dashboard knows about — Docker volumes, compose stacks, deployments, repositories, saved databases, the proxy's configuration and the dashboard itself, each drawn as its product — each protected by a named job or carrying a **Back up** button that opens the form pre-filled with its paths, SQLite file, native dump and the containers to pause. A job is its own page (`/backups/<id>`, and the old `?job=` redirects to it) with four readings — stored archives, bytes held, last run, next run — its facts, its verbs (run, edit, pause/resume, test destination, delete, declared once in `backups/job-verbs.tsx` and shared with the list) and its runs; a run offers its log, an archive browser with per-entry selection, restore into a directory or back in place, database restore into the connection or a drill database, the recovery check and a download. The form picks sources from the same coverage list, builds the schedule from presets with the sentence and next firings under it, keeps by count and by age, and lists the containers to freeze during the archive. `?source=<path>` opens the form with that path | backups page plus `components/backups/` and `components/schedule-builder.tsx` (the schedule builder, shared with a deployment's schedules); backend contract in [`../backend/git-backups-users.md`](../backend/git-backups-users.md#backups) |
| `/databases/*` | The current connection and its engine mark as a compact switcher beside its facts and actions, with the section's pages on the rail (registered through `nav-scope.tsx`, so a non-SQL engine's panel drops the pages it has no use for and every entry carries `?conn=`); Browse and Structure as one full-height workbench each — a shared table rail (`table-rail.tsx`, keyed per connection so one engine's schema filter cannot empty another's catalogue, and saying nothing about row count where the engine has no estimate rather than drawing "no rows") beside the grid or the column/index/key/definition view — with every table verb declared once in `table-actions.tsx`; the grid's export carries the conditions and the order on screen and names its row cap in the menu item that spends it, a cell opens its full value from the keyboard as well as the pointer, selecting rows is a read (it is how they are copied out) while only Delete needs the key and the capability, and the footer pages with First, a page number you can type, Next, Last once the table has been counted, and a Refresh — with each row's menu offering the tables that reference it (`/databases/{id}/relations`), which open filtered to that row; the ER diagram as a workbench with an inspector, a per-table menu (browse, structure, query, focus, isolate, hide, colour, note), search-and-pan, three levels of detail, layout direction and spacing, tidy, snap, lock, full screen, and export as PNG, SVG, Mermaid, DBML or JSON, with its arrangement saved per connection and schema (`diagram/memory.ts`); the key and document browsers drawn as the same workbench — Redis a key rail beside its typed value editor, Mongo a collection rail beside its documents under an underlined view strip (Documents, Indexes, Aggregate), its collection verbs behind one menu that names the export's row cap the way the table's does — rather than framed panels on a page that scrolls; query with a URL-supplied statement, cross-table find, monitor, ORM generation, and a connection page of readings, the connection string (masked, shown or copied on request, plus the public-address form for a server on this host), facts and maintenance rows — a dump, an Open to the internet switch for a container this host publishes, Remove only for a connection the sync would not re-add, and Delete with the option to remove the container and its data. New database starts an engine in a container with a Reachable from anywhere switch that is on by default; its engines, the connection dialog's and the deployment quick setup's are one `EngineCard` with the engine's logo. Every dialog is built from `components/form.tsx` and shows the statement it will run | `components/database/` and database layout/pages |
| `/deploy`, `/deploy/notifications`, `/deploy/credentials`, `/deploy/new`, `/deploy/[id]` (+ `/deployments`, `/logs`, `/runtime`, `/console`, `/players`, `/game-settings`, `/settings/{general,build,runtime,variables,domains,storage,databases,automation,danger}`), `/deploy/[id]/runs/[run]` | Projects under four readings (live, requests a minute, the share failing, build slots), in-progress runs, an attention list (a failed deploy with the engine's reason, a live release failing its health check, a site failing 5% or more), search and counted state chips — among them **Pull requests**, counting the projects with one open from the fleet-wide `GET /deploy/pull-requests` read (`fleet.ts`) — the cards ordered worst first and each drawn as its product with its source, last commit, hour of traffic and last fourteen runs, a card's source line saying "N pull requests · M previews" on the pull request glyph when the repository has any (`fleet-card.tsx`, `PullLine`), and the archived list — each project drawn as what it deployed — with Restore and Delete permanently (which asks for the name); fleet-level notification channels under four readings (channels, delivered and failed in the last day, the last message), drawn as their services in a picture and as cards ordered failing first with their last fourteen attempts, each opening its own sheet of attempts linked to the runs they announced (Send test disabled while paused, Create waiting for the fields the server requires, a signed webhook's secret shown once with the command that verifies it); saved credentials (Git tokens, SSH keys, registry logins, provider tokens — sealed, testable against a repository or image, refused for removal while in use) as cards under four readings (held and across which hosts, in use, never used, last used), each drawn as the host it signs in to and naming the projects that read through it, with the GitHub App's section above them (create through GitHub's manifest flow, installations drawn as their accounts' pictures, a three-segment setup path, install-elsewhere link, webhook address, disconnect), an add/edit sheet that names a pasted token by its prefix and a key by its first line, and a removal whose sentence says whether projects still use the credential; one-page project creation from a Git repository, image, template, database or Compose stack, every template card saying in one word how the first person gets in and the chosen one repeating that promise above its fields (`deploy/first-sign-in.tsx`, from the definition's own `access` block), the five sources reached only through the strip (the Git tab's duplicate grid of the others is gone), each source at the page's own left edge and one measure (the sources that opened in a centred 672px column no longer move the page's edge when a tab is pressed), with unfinished setups behind a counted button beside the question, one line each and discardable — and discarded automatically when a second source is inspected in their place; the whole chooser and each configure step held to the window at `xl`, each source drawn as the same two columns (the focused surface and a 22rem column beside it), with only the list or form longer than the space scrolling inside its own surface; the template catalogue shelved by what a template is for — topic chips with counts over cards of one size, each carrying its product's own logo (`product-logo.tsx`, files bundled in `public/logos/` with their `NOTICE`) — and the database engines and the images on this server drawn with theirs, each engine saying what kind of store it is; the plan drawn beside the form as source → build → runtime → address in the same vocabulary the project overview uses, each step pressable to open the fields that decide it and the runtime step reading out an unbounded memory limit; the detected framework named, a name a live project already holds flagged while it is typed rather than refused at commit, what detection could not answer said rather than swallowed, an image's exposed port seeded from its own configuration and its project type selectable so a container can be called a web application (which earns it candidate-first activation and a readiness gate), all eight build recipes offered, clone options governing every import on the tab, more than one hostname before the first deploy, automatic deployment and its watch paths decided at creation instead of after the first unintended release, the variables the source reads prefilled with their example placeholders and sources and arriving configured where detection set them up (a self-issued secret generated on the server when the project is created, an address that follows the domain, a documented default, a secret to paste; browser-compiled names tagged public, a localhost value flagged, a Generate button for a self-issued name typed by hand and never for a provider's), each detected database offered as one button (PostgreSQL with pgvector or PostGIS when the schema needs it, in the connection shape the application parses, and a hosted-only driver asked for its provider's string instead), the single-page switch for static output, a Python interpreter field, the Git tab's GitHub identities drawn as the accounts they are — one row with the account's own picture when the App is installed on the account the CLI is signed in to, two when they differ (pictures served through `GET /git/github/avatar`, since the page's `img-src` is its own origin) — with what each grants, how many repositories it is responsible for, and how to repair whichever is missing (the App is read from `GET /deploy/github-app/` rather than inferred from a repository count, and a GitHub-side failure degrades to a line rather than an error block), the picker's rows grouped under the identity that clones them, each carrying its owner's picture, and ordered by last push, with archived and fork marked and an import in flight lit round its edge, the Clone URL, Compose Git URL and Image reference fields drawing their host's logo as it is typed, Compose from Git picking a saved credential by name, Advanced storage using the Storage settings' own mount rows (a mount added there defaulting to managed), the import panel saying so with a link to Credentials when no GitHub identity is connected, a password switch on the public address, and `?repo=` prefilling the Git tab from a deploy link, `?template=` and `?image=` arriving on those tabs with a blueprint or image chosen (where the project step sends the upstream repository of a catalogued application), submodules and Git LFS switched beside the branch and turned on at inspection when the build root needs them, the candidate chooser ranked and saying why a candidate is not the application or cannot build as detected, the other processes the source runs and what detection set aside listed as folds, and a secret another platform's file generates set up to be generated on the server; the project shell (the identity line the host Overview opens on, Visit, one command, a verbs menu grouped Running, Building and Project — Restart and Stop, each asking first, Redeploy live release, Retry, Rebuild without cache, Deploy a specific version, **Test a pull request…** for an administrator of a GitHub-sourced project (a picker of the open requests, `pull-request-picker.tsx`, opening the same dialog the Git page uses), Duplicate project, Open in Docker — then Archive and Delete permanently) with every build it starts asking "Ready to deploy?" first when the advisory check (`POST …/check`) found what would stop it or warnings not yet confirmed, with Overview (the run in flight as its row; Before you deploy — what that check found, each finding opening its settings page; Production — the site preview with a phone-width switch beside the wiring drawn as products, over four live readings; First sign-in — the address, the user name to type, and, for a system administrator, Reveal on the credential the deploy generated; Needs attention; Delivery; recent deployments beside preview environments; and, for a github.com repository, **Pull requests** — the open requests each with the preview built from it, its tailnet address as a link once reachable and its state in one shared vocabulary (`lib/pull-requests.ts`: Pending, Building, Ready, Not reachable, Out of date once the head moved, Failed, Closing, Cleanup failed, Closed), then the previews whose request is merged or closed, with Test this pull request / Update preview, Merge (the dialog pins the head it read), Close preview (confirmed, no phrase) and Retry cleanup as row verbs declared once in `pull-request-verbs.tsx`, and a link to the checkout on the Git page; the panel hides itself when the server cannot read the repository and says why when the read is refused), Deployments (delivery insights whose failure reasons filter the list, run rows grouped by day with who started each as a face or a product and a duration bar, rollback through a dialog that draws the swap, What changed in this release, comparison, pin and older pages), Logs (five traffic readings, each carrying its hour, and an alerts line naming the channels each rule tells — the first rule added in a sheet on the page — over one pane of three views — Requests from the ingress's access log with status-family chips and counts, a path field holding a Pages-only toggle with the narrowings as chips, CSV export, the house chart of request volume with 4xx/5xx lines and the p95 under it, deploy/restart/exit marks and drag-to-narrow, rows coloured as the log console colours a request that open in place onto the container's output and events around that minute, with Copy as curl and Block in their menu, and a Live toggle that prepends; Insights, opening on a response-time ladder whose marks narrow to requests that slow, the window faceted as bar lists — pages with their own p95 and 5xx, clients with scanners named and a Block verb, agents with the bot share, referers, status codes, slowest, bytes served; Events, grouped under hour rules and each drawn on its product's tile — Docker's exits, OOM kills, restarts and health flips over containers and networks, searchable, narrowable by kind, followed live, scopable to a moment, each row linking to the run that made the release and — where the audit log explains it — to the entry that caused it, with the view and the moment in the URL), Runtime (services as cards of their image's product with readings joined from Docker, the four live usage tiles, domains with their issuer, storage beside backups, dependencies and usage history, each block saying when its owner could not be read), Console (one strip over a pane sized to the window), a game server's Players (Kick inline, operator and whitelist in a grouped menu, Ban confirmed) and Server settings (a railed form with each declared range inside its field, the raw file folded, and a save bar that follows) — the three game pages carrying one line of facts with the join address, the edition and how many are online — and nine settings pages as rail forms with a sticky Save per form (`settings/setting-card.tsx`; General's Source changes a repository, branch, root or image in place, offering what detection now reads differently field by field ("Detection changed"), the pending-changes strip naming what the check found about deploying the saved changes, and its Automatic deployment draws what a push does and the last decision; Build picks its builder from product cards of every recipe, names a Go module's main package, reads the source again with Detect again and applies what it proposes into the form, and opens, like Runtime, on readings from the draft and the live release; Variables is its list, its counts on chips, with an editor sheet and a `.env` import that previews each name before anything is written — a browser-compiled name defaulting to Build and Runtime (Build alone for static output), marked when it is not in the build, and a localhost value flagged; Domains' Add domain shows the record to create and whether it resolves here; Databases opens on four readings — linked count, connection, backup policy and native-dump coverage — over linked databases as lit cards in a section of their own, under a picture of how the application reaches them, carrying the engine, the database, the managed hostname, the observed status, a Test connection verb and, for an administrator, Copy application URL and Remove, with the variables that carry each database named and a removal that offers to delete them because runtime activation attaches a database by reading the variable rather than the dependency row; the backup job a release gates on is drawn as the Backups page draws it (`JobCard`: its products, last run, destination, last fourteen runs, next run and stored size) with the live release's observation folded onto the same card, Run now, and a warning naming any linked database the job takes no native dump of with one press to add it, or a **Back up** link to `/backups?database=<id>` when no job is declared at all; volumes carry their observed mount state; Automation opens on four readings and a picture of what deploys the project, with webhooks, schedules and previews as cards that open their own sheets; Danger zone is one frame of acts in lifecycle order); the deployment page opening on its identity line, with the release timeline, the build console (each step's lines under a sticky rule, an error count, elapsed time, a download), runtime logs with each source drawn as its product, details rows that open to each step's evidence (the plan check's findings drawn as findings), and metrics around activation; project cards and the project identity line carry the website's own icon; legacy `?tab=` links redirect | `components/deploy/` (`projects-page.tsx` with `fleet.ts` and `fleet-card.tsx`, `archived-projects.tsx`, `notifications-page.tsx`, `credentials-page.tsx`, `new-project.tsx` + `new-project/` (incl. `plan-wiring.tsx`, `automatic-deployment.tsx`), `project-context.tsx`, `project-shell.tsx`, `project-verbs.tsx`, `deploy-check.tsx` with `deploy-check-state.ts`, `pull-request-verbs.tsx`, `pull-request-picker.tsx`, `project-overview.tsx`, `overview-skeleton.tsx`, `usage-tiles.tsx`, `project-deployments.tsx`, `run-row.tsx`, `run-marks.tsx`, `run-verbs.tsx`, `project-logs.tsx`, `traffic-strip.tsx`, `requests-workspace.tsx`, `request-console.tsx`, `request-marks.tsx`, `request-chart.tsx`, `latency-ladder.tsx`, `traffic-facets.tsx`, `lifecycle-feed.tsx`, `add-alert-sheet.tsx`, `run-traffic.tsx`, `settings/traffic-alerts.tsx`, `project-runtime.tsx`, `service-product.ts`, `project-console.tsx`, `game/` (incl. `identity.tsx`), `run-page.tsx`, `run-pipeline.tsx`, `run-steps.tsx`, `run-metrics.tsx`, `run-logs.tsx`, `site-preview.tsx`, `project-mark.tsx`, `project-wiring.tsx`, `wire.tsx`, `https-toggle.tsx`, `settings/` (incl. `setting-card.tsx`, `setting-picture.tsx`, `detection-proposal.tsx` with `detection-changes.ts`, `segments.tsx`, `dotenv.ts`, `use-column-width.ts`, and `automation/` with `use-automation.ts`, `wiring.tsx` and `marks.tsx`), `vocabulary.tsx`); contract in [`../deployments/implementation.md`](../deployments/implementation.md) and [`../deployments/request-observability.md`](../deployments/request-observability.md) |
| `/docker/*` | Overview plus containers, images, networks, volumes, compose stacks, event history, creation, diagnostics, and resource detail. Every page is drawn the way the host Overview is — readings and hairline lists on the page, no framed block anywhere, each block rising once when its data lands (`design-system.md` §15). The overview names what is not running rather than only counting it, and draws disk as a breakdown; the containers and stacks pages both filter by state; the containers, images, volumes, networks and stacks lists are lit cards at every width (`design-system.md` §12), each carrying the product its image is as a logo (`product-logo.tsx` — a stack its services' products, a volume the product of the container that mounts it), the containers grouped under what needs attention, what is running and what is not, with their live readings beside the name on a wide screen and beneath it on a narrow one; and each container's and stack's verbs are offered as words — declared once in `container-actions.tsx` and drawn by the card, the stack's overflow menu and the detail page's compact context row alike. A container (`/docker/containers/<id>`, with `?tab=`) and a compose stack (`/docker/stacks/<name>`) are destinations rather than sheets: each holds a live log feed, and the container a shell and the stack a compose editor. Storage is browsable where it is named rather than a page away: a volume's panel, a container's Storage tab and a stack's Files tab all embed the same browse-and-edit file browser (`files/inline-browser.tsx` — drawn as the file manager's own listing in a pane: a path bar, thumbnails and kind marks, Name/Size/Modified/Owner columns that give way with the pane's width, a parent-folder row, a list or grid view remembered for the session, and a footer counting folders and files; walking in, opening a file in the Monaco editor with save, or in the media viewer, clamped to the mount's own root, with **Open in Files** carrying whichever directory was reached into `/files`). Reaching a volume's contents at all needs `/var/lib/docker/volumes` bind-mounted into the backend at its real path; without that entry in `docker-compose.yml` every mountpoint answers `not found` | `components/docker/`; see [`features-terminal.md`](features-terminal.md#feature-panels) |
| `/files` | One framed workbench with no page header (design-system §15, §14): a strip across the top holding the folder being browsed in its colour, its path and star, and every page command — Find, content search, view, arrange, the GitHub account, New and Upload; a fixed sidebar of places — home, the roots, `/etc`, `/var/www`, the accounts — then the starred folders and the recent ones, which stays put while the listing walks into folders and marks the one being browsed (`files-sidebar.tsx`; the same list is one menu on a phone, `places-menu.tsx`); a listing as details or tiles with image thumbnails and video posters on every row (`thumbnail.tsx`), Ctrl/Shift selection, arrow-key navigation, a right-click menu whose verbs are declared once (`file-actions.tsx`) and a folder menu between rows; drag-and-drop between rows, crumbs and sidebar places (`dnd.ts`); uploads of files and whole folders with per-file progress and a replace/keep-both/skip question for taken names (`uploads.tsx`, `conflict-dialog.tsx`); folders and files drawn as themselves (`file-icon.tsx` — a folder in the colour it was labelled, stored on the server and picked in the inspector, the strip or any folder's menu, with what it holds pressed into it; a file as a page carrying its format's logo and extension); a resizable inspector that shows the thing large, and the browsed folder while nothing is chosen; a full-screen viewer for pictures, video, audio, PDF, text and archives (`media-viewer.tsx`); Monaco editing with full screen and a diff review before saving; image editing, archives, permissions, a footer with the disk's free space, and Git/shell handoffs | files page and `components/files/`; checked by `tests/browser/files-ui.spec.ts` |
| `/git` | A list of repository cards rather than a table, each carrying the name, the branch it is on (`marks.tsx`, drawn with MDI's real git glyphs since Heroicons has none — `glyphs.tsx`), where on disk, the last commit as a forge draws one (the author's coloured mark, the short sha, the subject, who and when), how far the branch stands from its upstream, and the working tree as the five-square diffstat (`lib/git-status.ts`, `workingTreeSquares`) with its count beside it. **No readings across the top**: the operator asked for the four stat tiles to go, and the counts live on the filter chips, which both say what is waiting and narrow to it; the cards are ordered worst-first — conflicts, detached, uncommitted, behind, unpushed — under two rules, *Needs attention* and *Up to date*. A card is a choice, not a reading, so it carries the lit edge (design-system §16). Under those two lines, when GitHub has open pull requests for the checkout (`GET /git/pull-requests`, polled once a minute, each checkout read as its owner), a strip of up to three of them (`pull-request-row.tsx`: author, title, head → base, the folded check state, the preview's state where a deploy project built one) with "and N more" opening the workspace's GitHub tab, each row its own choice inside a container that stops the press reaching the card, with Test this pull request / Update preview, Merge and Open on GitHub as its verbs (`test-pull-dialog.tsx`: the exact head, a checkbox for production's variables — on by default for a same-repository head, off and disabled for a fork — and, for a fork, a warning that its code runs on this server once approved with an "I have reviewed this fork's changes" box gating the button; `merge-pull-dialog.tsx` pins the head it read); a **Pull requests** filter chip counts the checkouts with one open, and the search box matches a request by number or a word of its title; a card whose summary failed says `pull requests unavailable` with the reason. The chosen repository lives in `?repo=` and a pull request opened from a card in `?pull=`, which opens the workspace on its GitHub tab; clone and init sit behind **Add repository**; then one framed workbench per repository: the file tree, a Changes / History / Branches / GitHub column and a preview column separated by hairlines, resizable — and with a floor: each column is fitted against the measured row so neither can be dragged over the preview, which used to collapse to nothing because the widths were stored unclamped. Diffs here carry a forge's two line-number gutters and a row tint (`files/diff-rows.ts` decides what is drawn, `files/diff-view.tsx` paints it; the terminal's narrower column keeps the plain form). The repository's reading and its verbs sit in one strip. Changes lists a file on both sides when it is staged and edited again, keeps stashes at the top, deletes untracked files on discard, offers amend and the committer identity; History searches, pages and narrows to one file, and each commit opens as its message and file list with per-file diffs, branch/tag/cherry-pick/revert/undo verbs; Branches carries merged/gone facts, switch, check out a remote branch, merge, compare, rename, delete here or on the remote, and the tags; GitHub carries the pull requests with review and check state and, where a deploy project built a preview from one, where it stands (from the same summary, since gh knows nothing of deploy projects), merge and check-out, Test this pull request (held while a preview is Ready at this commit, Building, Closing or its cleanup failed, as on the Overview), Retry cleanup and Close preview, the repository's open issues under the requests (`GET /git/github/issues`, each with a Comment verb opening `comment-dialog.tsx`, which posts to `/git/github/pulls/{n}/comment` as the checkout's owner), the Actions runs on the branch and the new-request form with what it would carry; the preview adds selected-line/chunk staging, base/current/incoming conflict resolution, signature checks, blame, changed-file comparisons, GitHub conversations/reviews, a pull request's check runs read against its head (`GET /git/github/pulls/{n}/checks?head=`, each run a dot and a word with a link to its details) with a Comment verb, and job/step logs. The existing More menu opens recovery, worktrees, local history editing, submodules, Git LFS, patch exchange and GitLab/Gitea requests with account setup. History exposes its author filter, amend supports message-only edits, and the existing clone/remote/branch forms include clone options, URL editing and upstream tracking | `components/git/`; backend contract in [`../backend/git-backups-users.md`](../backend/git-backups-users.md#git-working-copies) |
| `/logs` | One-frame workbench: hideable, resizable source rail (each source drawn as its product) beside a single pane with Live/History tabs, one filter with level chips and counts, histogram, columned line console whose lines are coloured by their shapes (levels, programs, addresses, request lines, statuses, failure words) with structured lines drawn as their message and fields and a toggle back to the raw text, retention footer, and export | logs page, `components/logs/` (`log-text.tsx`), `lib/log-tokens.ts` |
| `/metrics` | Redesigned per design-system §15: the identity line (the processor drawn as its vendor's mark, cores, memory, swap, hypervisor, distribution, recording cadence), ten headline tiles read from the newest frame — CPU, memory, load, pressure, network, disk I/O, storage, sockets, processes, and the hottest sensor or the open file handles — each carrying a delta against the previous window on a recorded range; the recorder's findings as a plain list; a notable-moments list (peaks, stalls, failed deploys, reboots) whose rows zoom the charts; top processes by CPU, memory or I/O, each drawn as its product where it is one; plain utilisation and saturation charts (live and recorded, storage/inodes, annotations, range stack, synchronized cursor); and per-core, temperature, filesystem and interface readings. A dragged span is mirrored into the URL as `from`/`to` so a window is a link, the live feed can be paused, and the window on screen exports as CSV | metrics page, `components/metrics/` (`notable-moments.tsx`, `top-processes.tsx`, `hardware-panels.tsx`), `lib/metrics-summary.ts`, `lib/metrics-export.ts` |
| `/packages` | Drawn per design-system §15: the manager, the index age and the update verdict as a facts row under the title; four readings (installed, installed by hand, updates, on disk — naming the largest package); security updates waiting, a reboot owed and a stale index as notices that carry their own button; and three views under one underlined strip, each a plain panel — the installed table with by-hand/everything/behind chips and a size sort, the updates table with its upgrade-all verb, and the live catalogue search with a one-press install per row — with a row's tags at its edge, a sheet of facts and usage behind every name, and resumable jobs | packages page, `components/packages/`, shared job console; checked by `tests/browser/packages-ui.spec.ts` |
| `/processes/*` | Four routed pages listed on the rail's Processes panel. Live: four host readings (processes, blocked, zombies, unmanaged) over the inventory table with owner chips, user/state facets and an automatic focus that says why; a row's verbs (terminate, kill, pause/resume, hang up, open its supervisor, copy) in one menu; a detail sheet addressed by `?pid=` with listening ports, connection count, open-file limit, meters, a priority stepper and a Tree tab of the parent chain and children. PM2: whether each account's daemon resurrects on boot (hook and last save), four readings, a table with start/reload/restart/stop inline and logs, scale, reset counters, flush, delete behind the menu, a detail sheet of how it runs, a start-application dialog that shows the `pm2 start` command it will run, and daemon-wide save/reload/restart/stop. Services: four readings, state chips and a startup filter, failed units first, clear-failed and enable/disable, a sheet with reload where the unit supports it and a link to its unit file, plus `daemon-reload`. Scheduled: one account's cron jobs as rows with the schedule in words and its next run, per-job enable/disable/edit/remove and an add-job dialog with presets, systemd timers with run-now, and the read-only system cron files | processes layout and pages, `components/procs/`, `components/verbs.tsx`, `lib/cron.ts`, `lib/crontab.ts` |
| `/proxy/*` | Overview with the engine as a facts row (version, unit state, config directory, certbot, renewal), test/reload/restart/start/stop through the Services API, four destination tiles, an attention list folded from expired/expiring/unreadable certificates, a stopped renewal timer, disabled or plain-HTTP sites, unread streams and database ports on a public address (`components/proxy/attention.ts`), and the first sites; Sites as four readings over one filterable table whose verbs are declared once in `site-verbs.tsx` (edit, raw config, open, TLS report, access log, duplicate, enable/disable, delete) with a Docker Caddy route listed without an editor it cannot use, a site form built from `components/form.tsx` that links to issuance when it names a certificate the dashboard has not seen, and password files; Certificates as four readings (count, expiring, expired, renewal) with certbot lineages and their verbs, a staging-passed handoff to the real issuance, `?issue=` prefill, the timer switched on from the page, installed certificates with which site uses each, watched domains with a port, and DNS providers with credential state and removal; a TLS report that runs from `?domain=` with the grade as a tile and findings as a list; streams and ports as readings, filters and verbs, with a port's process one click away | proxy layout/pages and `components/proxy/` |
| `/security/*` | Shared posture, firewall and exposure context (the exposure carries the address this browser arrived from); an overview opening on how the panel is reached as an identity line (Tailscale's mark on a tailnet-only panel, the grade in its verdict's colour, the allowed ranges, interfaces and your own address as facts, the posture's verdict at the right end), five area tiles with the figure each area measured and the products it counts, and the posture's findings with their remedies; every area page as readings, findings and tables — the firewall opening on its backend with the enable switch beside its state, then defaults and rules; sshd opening on its port and what holds the listener, then staged settings and the keyed accounts drawn as people; fail2ban opening on its own mark, then jails as cards drawn as the service each watches (opening a sheet with a manual ban and a one-press allowlist for your own address), repeat offenders and the ban log; live connections with each address drawn as its network and each process as its product; the login record folded into attackers with their attempts as meters; the network inventory with each device drawn as what made it; and twenty filterable probes that other pages can arrive at with an address filled in. Any address in any list is a block at the firewall or one of three lookups | security layout/pages and `components/security/` (`marks.tsx`) |
| `/system-users` | Host account inventory, create/update/delete, groups, lock state, and SSH keys | system-users page; backend contract in [`../backend/git-backups-users.md`](../backend/git-backups-users.md#host-users-and-ssh-keys) |
| `/terminal` | Direct PTY sessions, folders, windows with retained screens and connections while switching, tabs and rows named after what the shell is doing (without the agent's own title glyph) with a working mark and the running program's own logo (a terminal's where it has none), a Files and Diff companion column (the Diff tab is the repository's changes read file by file, with a link to the Git page for everything else), the last-open session and window remembered across navigation, side tools, replay, clipboard upload, renderer, and keyboard customization | terminal page, `components/terminal/`, `components/xterm-pane.tsx`; see [`features-terminal.md`](features-terminal.md#the-terminal-panel) |

The project identity line and Settings → General show read-only production Git monitoring status,
including repository access failures. Branch deployments are automatic after the first deployment;
additional webhooks remain separate integrations. Run labels use a per-project sequence starting at 1,
while permanent run URLs retain their global identifiers. `/deploy/notifications`
(`components/deploy/notifications-page.tsx`) manages Discord, Slack, Telegram, e-mail and signed
webhook channels with event selection, pause/resume, test delivery and delivery history; the Automatic
deployment section on General carries the GitHub commit-status switch. Settings → Runtime exposes memory,
CPU and process limits, the restart policy and the health-check editor. The Console tab
(`components/deploy/project-console.tsx`) embeds the Docker exec pane on the live release container
for every non-game deployment. The shared vocabulary (`components/deploy/vocabulary.tsx`) is the one
place a run state, a project state, a source line or a release path is put into words, and the one
place a project is resolved to the product it is drawn as (`projectProduct`, `sourceProduct`); a
project's verbs are declared once in `project-verbs.tsx` and a run's in `run-verbs.tsx`, for every
surface that draws them. What a pull request and its preview say about themselves — the tone of the
folded check state, whether the head moved past the built revision, the one word for where the preview
stands — is decided in `lib/pull-requests.ts` so the Git page's card, the project Overview and the fleet
card cannot disagree, and the verbs a deploy project offers a pull request are declared once in
`deploy/pull-request-verbs.tsx`.

Cross-feature navigation is intentional: Files and Docker can open Git or a terminal in context; Git can
open GitHub authentication; deployments embed scoped runtime logs, service metrics, and automatic live
website previews while retaining links to owning Docker, proxy, backup, files, and terminal surfaces.
Query parameters that trigger a one-time terminal/file context are consumed
so refresh does not repeat an action.
